export const SEARCH_RADIUS_M: number = 5_000;

// Inlined at build time by Expo; restrict this key to the Places API +
// Maps SDK for Android in the Google Cloud console since it ships in the binary.
export const GOOGLE_MAPS_API_KEY: string = process.env.EXPO_PUBLIC_GOOGLE_MAPS_API_KEY ?? '';

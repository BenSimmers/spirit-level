# Google Cloud API key with "Places API (New)" and "Maps SDK for Android" enabled.
# EXPO_PUBLIC_ vars are inlined into the app bundle — restrict the key to those
# two APIs and to this app's package name / bundle id in the Cloud console.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=""
